# V1 archive

This folder preserves the completed first version of the label generator as it stood when V2 work began on 2026-07-26.

V2 lives at the repository root. The `imgs/` asset library remains local and gitignored, so this archive may still need that local library to render every historical logo.
